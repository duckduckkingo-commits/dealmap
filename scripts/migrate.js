// Prints migration guidance. Real migrations live in supabase/migrations/*.sql
console.log("Apply SQL in supabase/migrations/ to your Supabase Postgres (dashboard > SQL editor or supabase db push).");
console.log("Local dev uses data/db.json and needs no migration.");
