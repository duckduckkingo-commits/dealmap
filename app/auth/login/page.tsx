import { AuthForm } from "@/components/AuthForm";
export const metadata = { title: "Log in — DEALMAP", robots: { index: false } };
export default function Login() { return <AuthForm mode="login" />; }
