import { AuthForm } from "@/components/AuthForm";
export const metadata = { title: "Register — DEALMAP", robots: { index: false } };
export default function Register() { return <AuthForm mode="register" />; }
