// Flat-config stub. Install `eslint` + `eslint-config-next` to enable
// full `next/core-web-vitals` linting; `npm run lint` (scripts/lint.js)
// runs offline until then.
/** @type {import('eslint').Linter.Config[]} */
const config = [];
export default config;
